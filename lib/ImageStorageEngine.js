"use strict";

module.exports = class ImageStorageEngine {
    constructor({ destination: this.getDestination } = { destination }) {

    }

    _handleFile(req, file, cb) {

    }

    _removeFile(req, file, cb) {

    }
}
